<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/edit_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/edit_image"
        android:layout_alignStart="@+id/edit_image"
        android:layout_alignEnd="@+id/edit_image"
        android:layout_marginStart="-118dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="-106dp"
        android:hint="Name"
        android:inputType="text"
        android:minHeight="48dp" />

    <EditText
        android:id="@+id/edit_address"
        android:layout_width="395dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/edit_name"
        android:layout_alignStart="@+id/edit_name"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="0dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="0dp"
        android:hint="Address"
        android:inputType="textLongMessage"
        android:minHeight="48dp" />

    <EditText
        android:id="@+id/edit_contactnumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_address"
        android:layout_alignStart="@+id/edit_address"
        android:layout_alignEnd="@+id/edit_address"
        android:layout_marginStart="0dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="0dp"
        android:hint="Contact Number"
        android:inputType="number"
        android:minHeight="48dp" />

    <EditText
        android:id="@+id/edit_emailaddress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_contactnumber"
        android:layout_alignStart="@+id/edit_contactnumber"
        android:layout_alignEnd="@+id/edit_contactnumber"
        android:layout_marginStart="0dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="0dp"
        android:hint="Email Address"
        android:inputType="textEmailAddress"
        android:minHeight="48dp" />

    <ImageView
        android:id="@+id/edit_image"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="118dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="106dp"
        android:scaleType="centerCrop"
        app:srcCompat="@drawable/user" />

</RelativeLayout>